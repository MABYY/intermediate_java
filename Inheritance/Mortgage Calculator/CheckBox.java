package com.codewithmosh;

public class CheckBox extends UIControl{
    // ERR: There is no default constructor available in 'com.codewithmosh.UIControl'
    // This is because UIC has a contructor, so we should extend it using super()
    // The alternative is to comment it out

}
